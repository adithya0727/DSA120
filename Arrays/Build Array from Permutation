/* Approaches :
Input: nums = [0,2,1,5,3,4]
Output: [0,1,2,4,5,3]
1) create a new vector and run a for loop to assign values. 
*/

1) vector<int> buildArray(vector<int>& nums) {
        int n = nums.size();
        vector<int>ans(n);
        
        for(int i=0;i<n;i++){
            ans[i] = nums[nums[i]];
            
        }
        return ans;
    }
