/* Approaches:
1)loop 
2) understood about using (long long) to pass all test cases and have more precision. n*n+1/2
*/

1) long long seriesSum(int n) {
	    int num = 0;
	    for(int i=0;i<=n;i++){
	        
	        num += i;
	        
	    }
	    return num;
	}


2) long long seriesSum(int n) {
	    long long sum = ((long long)n*(long long)(n+1))/2;
	    return sum;
	}
